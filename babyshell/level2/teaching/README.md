The previous shellcode works in this problem, which means that it is position-independent.
For `lea` operation, it uses relative address with the register `RIP`.
That's why it is also working for this problem.
